{
  "name": "ion2",
  "version": "1.0.3",
  "description": "Indentational Object Notation: a concise way to represent JSON object",
  "main": "lib/ION.js",
  "scripts": {
    "compile": "babel --source-maps -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "author": "Yue Marvin Tao",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
